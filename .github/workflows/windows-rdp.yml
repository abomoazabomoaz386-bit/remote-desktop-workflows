name: WINDOWS RDP - CLOUDFLARE

on:
  workflow_dispatch

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Setup Cloudflared
        uses: AnimMouse/setup-cloudflared@v2

      - name: Enable Windows RDP
        run: |
          Write-Host "Killing any existing RDP sessions..."
          # Try to kill sessions safely
          tsdiscon 0 /server:localhost 2>$null
          tsdiscon 1 /server:localhost 2>$null
          tsdiscon 2 /server:localhost 2>$null
          
          # Enable RDP
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -ErrorAction SilentlyContinue
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop" -ErrorAction SilentlyContinue
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1 -ErrorAction SilentlyContinue
          
          Write-Host "✅ RDP Enabled!"
        continue-on-error: true

      - name: Set Administrator Password
        run: |
          Set-LocalUser -Name "runneradmin" -Password (ConvertTo-SecureString -AsPlainText "P@ssw0rd123!" -Force)
          Write-Host "✅ Password Configured!"

      - name: Start Cloudflare Tunnel
        run: |
          Write-Host "============================================"
          Write-Host "🚀 WINDOWS RDP - CLOUDFLARE TUNNEL"
          Write-Host "============================================"

          # استخدام طريقة أكثر استقراراً
          cloudflared tunnel --url tcp://localhost:3389 --protocol http2 > tunnel.log 2>&1 &

          Write-Host "⏳ Waiting for tunnel (35 seconds)..."
          Start-Sleep -Seconds 35

          $url = Select-String -Path "tunnel.log" -Pattern "https?://[a-zA-Z0-9-]+\.trycloudflare\.com" | Select-Object -First 1

          if ($url) {
              Write-Host "✅ TUNNEL URL: $($url.Matches.Value)" -ForegroundColor Green
          } else {
              Write-Host "⚠️ لم يتم العثور على URL. Log كامل:" -ForegroundColor Yellow
              Get-Content tunnel.log -Raw
          }

          Write-Host ""
          Write-Host "📋 CONNECTION DETAILS:"
          Write-Host "   USERNAME: runneradmin"
          Write-Host "   PASSWORD: P@ssw0rd123!"
          Write-Host "============================================"

          while ($true) { Start-Sleep -Seconds 60; Write-Host "✓ Active" }
          while ($true) {
            Start-Sleep -Seconds 60
            Write-Host "✓ Active - $(Get-Date -Format 'HH:mm:ss')"
          }
